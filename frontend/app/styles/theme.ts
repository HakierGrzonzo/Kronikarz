import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#A08D8D",
    },
    secondary: {
      main: "#888798",
    },
    background: {
      default: "#E0EEED",
    },
  },
});
