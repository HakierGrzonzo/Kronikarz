{
  "name": "frontend",
  "scripts": {
    "dev": "solid-start dev",
    "build": "solid-start build",
    "start": "solid-start start",
    "openapi": "openapi --input http://localhost:8000/openapi.json --output ./src/client --client fetch --name AppClient",
    "openprod": "openapi --input ./openapi.json --output ./src/client --client fetch --name AppClient",
    "pre-commit": "prettier -w ."
  },
  "type": "module",
  "devDependencies": {
    "openapi-typescript-codegen": "^0.23.0",
    "solid-start-node": "^0.1.0",
    "typescript": "^4.8.3",
    "vite": "^3.1.0"
  },
  "dependencies": {
    "@solidjs/meta": "^0.28.0",
    "@solidjs/router": "^0.4.3",
    "prettier": "^2.7.1",
    "solid-js": "^1.5.7",
    "solid-start": "^0.1.0",
    "undici": "^5.10.0"
  },
  "engines": {
    "node": ">=16"
  }
}
